const a = 2;

a = 4



console.log(a);

// re-assignment to const is not allowed

/*
    var - you can declare and re-declare
    let - you cannot re-declare it again in the same block
    const - you cannot re-declare it again in the same block or re-assign its value
*/