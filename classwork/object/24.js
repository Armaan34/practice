var { data } = require("./data.js")

function getGeo(fname){
    //var result = data.filter(ele => ele.name == "Glenna Reichert").map(ele => ele.address.geo)
    //console.log(result[0]);

    var output = data.find(ele =>)
}
getGeo("Glenna Reichert")